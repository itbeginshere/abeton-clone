const LandingHeader = () => {
    return (
        <header className={'bg-landing-image h-landing-image bg-center bg-no-repeat bg-cover flex flex-row items-center justify-center'}>
            <span className={'text-8xl text-custom-coral font-semibold'}>Ableton</span>
        </header>
    );
}

export default LandingHeader;